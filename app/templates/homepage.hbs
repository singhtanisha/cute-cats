{{#if rows}}
    <div class="cat-container">
        {{#vertical-collection rows estimateHeight=38 containerSelector=".cat-container" lastReached="loadNextPage" as |row|}}
            {{image-row row=row}}
        {{/vertical-collection}}
    </div>
{{/if}}
